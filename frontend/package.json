{
  "dependencies": {
    "express": "^4.14.1",
    "vue": "^2.1.10",
    "vue-material": "^0.7.1"
  },
  "name": "frontend",
  "version": "1.0.0",
  "main": "server.js",
  "author": "ymcatar",
  "license": "MIT",
  "scripts": {
    "start": "serve --port 5000 dist",
    "dev": "vbuild --config vbuild.config.js --port 5000 ./src/index.js --dev",
    "build": "vbuild --config vbuild.config.js ./src/index.js"
  }
}
